# 本地开发环境
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/hear_like_me?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: root
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password:
      database: 0
      timeout: 10s
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

hear-like-me:
  storage:
    root-path: D:/hear-like-me/data
    preview-url-prefix: http://localhost:8081
  engine:
    ffmpeg-path: ${HLM_FFMPEG_PATH:E:/hear-like-me/ffmpeg-9.0.1-essentials_build/bin/ffmpeg.exe}
    script-path: ${HLM_ENGINE_SCRIPT:../../audio-engine/cochlear_vocoder.py}
    vocoder-enabled: ${HLM_VOCODER_ENABLED:true}
