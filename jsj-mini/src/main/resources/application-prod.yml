# 生产 / 服务器环境
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:hear_like_me}?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: abc1653150573
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false

  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      username: root
      password: abc1653150573
      database: ${REDIS_DATABASE:0}
      timeout: 10s
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

hear-like-me:
  storage:
    root-path: ${HLM_STORAGE_ROOT:/data/hear-like-me}
    preview-url-prefix: https://43.143.30.134/
  engine:
    python-path: ${HLM_PYTHON_PATH:python3}
    script-path: ${HLM_ENGINE_SCRIPT:/opt/hear-like-me/audio-engine/cochlear_vocoder.py}
    timeout-seconds: ${HLM_ENGINE_TIMEOUT_SECONDS:120}
    ffmpeg-path: ${HLM_FFMPEG_PATH:ffmpeg}
